services:
  payment-api:
    build: .
    container_name: payment-api
    restart: always

    ports:
      - "3000:3000"

    environment:
      DB_HOST: payment-db.czqamc00o19t.ap-southeast-2.rds.amazonaws.com
      DB_USER: adminUser   # ⚠️ fix this
      DB_PASSWORD: ${DB_PASSWORD}
      DB_NAME: payments
      DB_PORT: 5432